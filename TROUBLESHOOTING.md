# Troubleshooting

常見問題與排查紀錄。

---

## 折線斷斷續續、有時整段不出現（用手機/喇叭播放歌曲時）

### 症狀 (Symptom)
- 用 `awe live` 或 `awe analyze` 對著麥克風播放**整首歌曲**（手機喇叭等）。
- 終端折線圖**斷斷續續**、只剩零星的點，有時整段**不出現**音準曲線。
- `analyze` 有時直接顯示 `No pitched audio detected`。

### 原因 (Cause) — 不是 bug，是偵測原理的先天限制
本工具用 **YIN** 演算法，它是**單音 (monophonic)** 基頻偵測器：一次只能鎖定**一個**清楚的週期音高。
而「整首歌」是**複音 (polyphonic)**：人聲＋鼓＋吉他＋貝斯…多個頻率同時響。

```
單音 (清唱 / 單一樂器)              複音 (整首歌：人聲+伴奏)
   一個清楚週期                       多個頻率疊在一起 → 無單一週期
        │                                   │
   YIN 找到基頻 ✅                     YIN 差異函數降不到門檻 ❌
        │                                   │
   每幀都有值 → 連成線              多數幀 = null → 線斷掉 / 不出現
```

畫面上：折線只連接「相鄰且都有偵測到」的欄位；複音時大多數幀為 `null`，
零星有值的欄位無法連線 → 看起來就是斷續的點，甚至整段空白。

加重因素：
- 手機喇叭 → 空氣 → 麥克風會**變小聲**、加入**房間殘響/噪音**，週期性更差、命中率更低。
- 實測佐證：先前的 `test.mp3`（歌曲）在預設門檻下 **212 幀僅 0 幀**偵測到音高。

### 解法 / 正確用法 (Resolution)
- ✅ **最有效**：對麥克風**自己清唱**，或用**單一樂器**（鋼琴單音、管樂等）— 這才是 YIN 的適用場景，線就會連續。
- ✅ **大聲、靠近麥克風、降低背景噪音**。
- ✅ 若一定要用歌曲，挑**幾乎只有人聲（清唱 / a cappella）**的片段。
- ✅ 可調參數（已開放）：`--tolerance` 調「準」的範圍；`--min-note/--max-note` 固定 Y 軸。

### 限制與後續方向 (Limitations)
- 「補小洞」(跨越短暫掉幀連線) 或放寬 `--threshold/--rms` 等，**只能改善清唱的小掉幀，救不了複音歌曲**。
- 要對「整首歌」抽取主旋律屬完全不同等級的工程：需神經網路音高偵測（如 CREPE）或先做**人聲分離**再追音高。

### 相關 (Related)
- 同一家族的症狀「`No pitched audio detected`」也可能來自：
  - 錄音檔是 **0 bytes 空檔**（下載失敗 / 錄音被提前中止）→ 先用 `ls -l`、`file`、`ffprobe` 確認檔案有效。
  - 錄到的是**安靜環境音**（低於靜音門檻）→ 實際出聲再錄。
- 相關檔案：`src/pitch/yin.js`（YIN 與門檻）、`src/render/chart.js`（折線連接邏輯）、`src/live.js`。
